# One-minute demo video plan (reformed & simplified)

**For the ~60-second recording.**  
Only the parts that still matter after your decisions. Everything else has been stripped or marked IGNORED.  
This is now a clean, copy-paste-ready reference skeleton.

---

## FLAG — locked decisions (use these exact strings)

**Team identity:** Team Vector  
**Product name:** Vector  
**Platform one-liner:** the ISR drone flight plan mapper  
**Core pain (single lead):** ISR operators plan complex branching drone flight plans, but with no real-time connection there’s no way to rehearse how the drone will actually respond when they give a simulated PEQ-15 cue at a decision zone.  
**Cue language:** “simulated PEQ-15 intent cue”  

**Everything below this line was ignored per your instructions (7–32 except the locked items above).**

---

## Updated Outline (modular blocks — target 55–62 s VO)

### A. Introduction (~8 s)
> “Team Vector presents **Vector** — the ISR drone flight plan mapper.  
> ISR operators plan complex branching drone flight plans, but with no real-time connection there’s no way to rehearse how the drone will actually respond when they give a simulated PEQ-15 cue at a decision zone.”

### B. Context / Problem (~12–15 s)
> “Every unexpected cue forces them to abort, replan, and lose critical time.  
> Vector changes that — it turns a carried route into a rehearsable, branching execution trace operators can trust before launch.”

### C. Solution (~15–20 s)
> “In Plan mode you build the full branching flight plan with decision zones.  
> Vector validates everything — battery, range, terrain, ruleset.  
> Then you hit Run and rehearse the entire mission against simulated PEQ-15 intent cues.  
> Same app, same workflow — no separate demo build.”

### D. Demo (~20–25 s)
(You said you’ll build this with what you have — just hit the loud beats in this order for max impact):
1. AOI / terrain establishing shot
2. Plan mode — waypoint queue + rich decision node
3. Validation step (quick flash of warnings/limits)
4. Run / rehearsal — approach to decision zone
5. Simulated PEQ-15 cue → Primary branch trigger (the wow moment)
6. Quick proof: map preview, audit line, timeline tick

### E. Close (~5 s)
> “Same planner a judge can edit live right now.  
> This is time-compressed — the real thing is interactive.”

---

## Narration timeline and script (~60 s)

**Notes:** `(VO)` is spoken narration. `(ON-SCREEN)` is what viewers should see; align labels with your shell (*Plan Mission* / *Launch Package Simulation*, etc., if wording differs). **Total ~120–135 spoken words** at a clear pace (~2.3–2.5 words/sec). If you run long on the demo montage, cut **SEGMENT 3** optional sentence first.

### Master timeline

| Clock | VO (spoken) | On-screen |
| --- | --- | --- |
| **0:00–0:02** | *(optional beat of silence / music sting; or start cold)* | Fade in: fullscreen map + dark operator chrome. Burn or lower-third: **Team Vector**. Product wordmark or toolbar title: **Vector**. |
| **0:02–0:09** | Team Vector presents **Vector**—the ISR drone flight plan mapper. | Establishing shot: cursor idle or slow pan over AOI terrain / context layers. |
| **0:09–0:22** | Operators plan branching ISR drone routes—but without a realtime link there’s no way to rehearse what actually happens when the team sends a **simulated PEQ-15 intent cue** at a decision zone. Uncertainty burns time before launch. | Slow zoom or layer toggle (roads / ridges / no-go if present). Hint at **branching geometry** on map (two route arms or queue). Avoid rapid UI hops—keep readability. |
| **0:22–0:35** | **Vector** fixes that. In Plan mode you build the full branching plan with decision zones. Validation catches battery, range, terrain, and ruleset issues. Same app—you’re not switching to a separate demo build. | Hard cut to **Plan** mode (toolbar / mode pill clearly reads Plan). Brief right-rail or panel: waypoint **queue** / outline. Click or highlight a **decision** waypoint or node with **Primary / Alternate** language visible if UI has it. |
| **0:35–0:42** | Then you hit **Run** and rehearse the whole mission against those simulated PEQ-15 intent cues. | Transition to **Run / simulation** (mode switch visible). **Start** or resume playback; show sim time or playhead advancing. |
| **0:42–0:52** | *(Demo-forward; shorter phrases—let picture lead.)* Here we’re approaching the decision zone—preview the branch—fire the cue—**Primary** commits. | **Run** playback toward **Decision Target Zone** (circle/zone visible if supported). Flash **validation / warnings** row for ~1 s if dense. Trigger **simulated cue** (PPS control or equivalent: e.g. **4 PPS** / Primary if buttons are on screen). Route **preview** snaps to Primary; **active branch** highlight on map. |
| **0:52–0:57** | Every transition is logged—audit trail and timeline stay in sync. | **Pull focus** to **audit log** line (new entry) + **timeline** tick / state change. One readable line of log text—no wall of text. |
| **0:57–1:00** | Same planner a judge can drive live—you just watched it compressed. | Optional: subtle zoom to full UI; hold **interactive** chrome. Fade or end card. |

### VO-only script (single block, for recording)

Team Vector presents **Vector**—the ISR drone flight plan mapper. Operators plan branching ISR drone routes—but without a realtime link there’s no way to rehearse what actually happens when the team sends a **simulated PEQ-15 intent cue** at a decision zone. Uncertainty burns time before launch. **Vector** fixes that. In Plan mode you build the full branching plan with decision zones. Validation catches battery, range, terrain, and ruleset issues. Same app—you’re not switching to a separate demo build. Then you hit **Run** and rehearse the whole mission against those simulated PEQ-15 intent cues. Here we’re approaching the decision zone—preview the branch—fire the cue—**Primary** commits. Every transition is logged—audit trail and timeline stay in sync. Same planner a judge can drive live—you just watched it compressed.

### Shot list tied to SEGMENT D (quick reference)

1. Establishing AOI / terrain.  
2. **Plan:** queue + rich decision waypoint; **validation** strip or panel flash.  
3. **Run:** advance to zone; **cue → Primary**; **map preview** update.  
4. **Audit** + **timeline** proof frame.  
5. Close hold on UI or end card.

---

## Timing cheat sheet (unchanged)

| Block     | Rough share of 60 s | Trim first          |
|-----------|---------------------|---------------------|
| Intro     | ~12–15%             | Proper names        |
| Problem   | ~22–28%             | Second sentence     |
| Solution  | ~25–33%             | Any extra fluff     |
| Demo      | ~40–52%             | Extra map glamour   |

---

## Judging alignment (still relevant)

- **Future potential** → carried plan + rehearsal + audit trail  
- **Technical implementation** → real Plan → Validation → Run moment + visible branch switch  
- **Creativity / uniqueness** → operator rehearsal against simulated PEQ-15 cues in a comms-denied environment  
- **Pitching quality** → tight arc: who / pain / fix / proof

---

**Next steps:**  
- Record VO from *VO-only script* (or table column); tighten if over 62 s.  
- Captions / music / end card specs.  
- Separate Q&A doc (if needed).

---

## Prominent keywords — demo coverage checklist

Use this as a sweep before record: VO, on-screen UI, captions, or visible map semantics should collectively hit each **must** row. **Should** rows depend on build state (omit from VO if absent).

### Identity & hackathon framing (must)

| Keyword / phrase | Included when… |
| --- | --- |
| Team Vector | Spoken or title card early |
| Vector (product name) | Spoken or persistent UI chrome |
| ISR | Clear in introduction (product is ISR-facing) |
| Comms-denied / no real-time link | Optional if already implied by VO pain line; reinforces “why rehearsal” |

### Modes & workflow (must)

| Keyword / phrase | Included when… |
| --- | --- |
| Plan (Plan Mission / Plan mode) | Visible mode chrome + VO |
| Run (Run Mission / rehearsal / simulation) | Visible transition + VO |
| Same app / same workflow — not a demo fork | Closing or solution block |
| Waypoint queue / mission outline | On screen in Plan beat |
| Branching plan / branching route | VO or visible Primary/Alternate (or equivalent) |

### Geography & packaging (must for rebuild path; adapt if Vite-only)

| Keyword / phrase | Included when… |
| --- | --- |
| Launch package (optional VO) | If UI says it; ties to squad-moving-overwatch narrative |
| AOI / terrain / context layer | Establishing shot |
| Decision zone / Decision Target Zone (DTZ) | Map + VO (“decision zone” matches locked copy) |

### Cues & semantics (must)

| Keyword / phrase | Included when… |
| --- | --- |
| Simulated PEQ-15 intent cue | Locked VO phrase |
| Decision point (where simulation pauses for cue) | Shown at pause / zone selection if product does it |
| Primary branch / Primary route | Wow beat + map highlight |
| Alternate (if demo shows second path) | At least one frame or legend if product has it |

### PPS grammar (should — if UI exposes 1/2/4/8)

| Keyword / phrase | Included when… |
| --- | --- |
| PPS / pulse | Button row or log line visible; VO may say “encoded pulse” or stay PEQ-15 only |
| `1` hold/loiter, `2` land/recover, `4` primary, `8` alternate | Match **legend or tooltips** to avoid judge correction |

### Proof surfaces (must)

| Keyword / phrase | Included when… |
| --- | --- |
| Validation / warnings (battery, range, terrain, ruleset) | Flash in Plan or pre-Run |
| Route or branch preview | Map update when branch applies |
| Audit log / audit trail | At least one line on screen or VO |
| Timeline / state or run log tick | Panel visible in Run beat |

### Tech & accuracy (should)

| Keyword / phrase | Included when… |
| --- | --- |
| MGRS and/or Lat/Lon readout | Status bar or selection panel if implemented |
| WGS84 / GeoJSON (optional VO) | Usually skip in 60s; Q&A only |
| AGL / default altitude | Only if altitude story is on screen |
| Provenance / source (optional) | Layer or panel if Palantir/static bundle context shown |

### Visual language (should)

| Keyword / phrase | Included when… |
| --- | --- |
| Waypoint behavior glyph / symbology | Readable on map or queue |
| Primary vs alternate route styling | Yellow-line grammar or labels if per design doc |
| No-go / attention terrain (if in scenario) | Layer toggle or visible geometry |

### Partner & scope honesty (conditional)

| Keyword / phrase | Included when… |
| --- | --- |
| Palantir / Foundry | Only if provider badge, data source, or live call is visible |
| Writeback / control real hardware | Only if shipped; otherwise keep story on rehearsal / synthetic workflow |

